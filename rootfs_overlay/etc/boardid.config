# boardid.config

# Read the serial number from the NervesKey
-b nerves_key -f /dev/i2c-1

# If the NervesKey isn't programmed, make it obvious. This will
# show up in the hostname.
-b force -f unprovisioned
